import fs from 'node:fs';
import path from 'node:path';
import {buildTargetVocabulary,buildSyllableInventory,targetVocabularyStats,isRebuloPriorityTarget} from '../src/target-vocabulary.js';
import {analyzeSyllableInventoryCoverage} from '../src/syllable-coverage.js';
import {buildRepresentationResearchQueue,representationResearchStats} from '../src/syllable-representation-candidates.js';
import {OPEN_PICTOGRAMS} from '../src/open-pictogram-library.js';import {OPEN_PICTOGRAMS_WAVE_2} from '../src/open-pictogram-library-wave2.js';import {OPEN_PICTOGRAMS_WAVE_3} from '../src/open-pictogram-library-wave3.js';
const input=process.argv[2]||'data/lexique4.compact.json',output=process.argv[3]||'data/target-vocabulary-report.json';if(!fs.existsSync(input)){console.error(`Lexique compact introuvable: ${input}`);process.exit(1);}const source=JSON.parse(fs.readFileSync(input,'utf8')),entries=Array.isArray(source?.entries)?source.entries:[],seed=JSON.parse(fs.readFileSync('data/lexicon-seed.json','utf8')),technicalInventory=[...seed,...OPEN_PICTOGRAMS,...OPEN_PICTOGRAMS_WAVE_2,...OPEN_PICTOGRAMS_WAVE_3];const targets=buildTargetVocabulary(entries),rebuloPriorityTargets=targets.filter(isRebuloPriorityTarget),syllables=buildSyllableInventory(targets),usefulSyllables=buildSyllableInventory(rebuloPriorityTargets),stats=targetVocabularyStats(targets),syllableCoverage=analyzeSyllableInventoryCoverage(syllables,technicalInventory),usefulSyllableCoverage=analyzeSyllableInventoryCoverage(usefulSyllables,technicalInventory),representationResearchQueue=buildRepresentationResearchQueue(usefulSyllableCoverage.rows,entries),representationResearch=representationResearchStats(representationResearchQueue);
const report={generatedAt:new Date().toISOString(),source:source?.source||'Lexique 4',sourceLicense:source?.license||'CC BY-SA 4.0',status:'editorial-preselection',caution:'Les niveaux d’âge et niveaux d’utilité Rebulo sont des présélections heuristiques, non des âges d’acquisition ni validations cliniques. Les statistiques globales Lexique restent informatives; les priorités produit utilisent d’abord le vocabulaire Rebulo utile.',selectionPolicy:{lexicalCategories:['NOM','VER','ADJ','ADV','ONO'],onePreferredFormPerLemma:true,utilityTiers:['very_common_simple','child_common','school_common','teen_adult_common','advanced_useful','lexique_background'],priorityObjective:'Rebulo useful coverage first; global Lexique coverage secondary',sourceSyllabificationRequiredForExactInventory:true},stats,globalLinguisticCoverage:{targetCount:targets.length,syllableInventoryCount:syllables.length,syllableCoverageCounts:syllableCoverage.counts},rebuloUsefulCoverage:{targetCount:rebuloPriorityTargets.length,syllableInventoryCount:usefulSyllables.length,syllableCoverageCounts:usefulSyllableCoverage.counts},technicalPictogramInventoryCount:technicalInventory.length,representationResearch,representationResearchQueue,syllableInventory:usefulSyllableCoverage.rows,targets};fs.mkdirSync(path.dirname(output),{recursive:true});fs.writeFileSync(output,JSON.stringify(report,null,2));console.log(`Built ${targets.length} target lemmas; ${rebuloPriorityTargets.length} drive Rebulo priorities -> ${output}`);console.log(`Global syllable coverage: ${JSON.stringify(syllableCoverage.counts)}`);console.log(`Useful Rebulo syllable coverage: ${JSON.stringify(usefulSyllableCoverage.counts)}`);
