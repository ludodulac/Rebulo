import {normalizeIPA} from './phonetic-engine.js';

export const TARGET_AGE_BANDS=Object.freeze([
  Object.freeze({age:5,label:'5–6 ans',minFrequency:50,maxSyllables:2,maxLetters:9}),
  Object.freeze({age:7,label:'7–8 ans',minFrequency:10,maxSyllables:3,maxLetters:12}),
  Object.freeze({age:9,label:'9–11 ans',minFrequency:2,maxSyllables:4,maxLetters:15}),
  Object.freeze({age:12,label:'12 ans +',minFrequency:0.5,maxSyllables:5,maxLetters:18})
]);

export const REBULO_UTILITY_TIERS=Object.freeze(['very_common_simple','child_common','school_common','teen_adult_common','advanced_useful','lexique_background']);
const UTILITY_WEIGHTS=Object.freeze({very_common_simple:6,child_common:5,school_common:4,teen_adult_common:3,advanced_useful:2,lexique_background:0});
const ALLOWED_POS=new Set(['NOM','VER','ADJ','ADV','ONO']);
function lexicalPos(value=''){return String(value||'').trim().toUpperCase().split(':')[0];}
function normalizedWord(value=''){return String(value||'').trim().toLocaleLowerCase('fr').replace(/[’]/g,"'").normalize('NFC');}
function letterCount(value=''){return Array.from(normalizedWord(value).replace(/[-']/g,'')).length;}

export function sourceSyllables(value=''){const raw=String(value||'').trim();if(!raw)return [];return raw.replace(/^[/\[]|[/\]]$/g,'').split(/[.·‧-]+/).map(normalizeIPA).filter(Boolean);}
export function ageBandCandidateForEntry(entry={}){const word=normalizedWord(entry.word||entry.lemma),frequency=Number(entry.frequency)||0,syllableCount=Number.isInteger(entry.syllableCount)&&entry.syllableCount>0?entry.syllableCount:null,pos=lexicalPos(entry.pos);if(!word||!normalizeIPA(entry.ipa)||!ALLOWED_POS.has(pos)||!/^[\p{L}'’-]+$/u.test(word)||!syllableCount)return null;const letters=letterCount(word);return TARGET_AGE_BANDS.find(p=>frequency>=p.minFrequency&&syllableCount<=p.maxSyllables&&letters<=p.maxLetters)?.age||null;}

export function rebuloUtilityTierForEntry(entry={}){const frequency=Math.max(0,Number(entry.frequency)||0),syllables=Number(entry.syllableCount)||99,letters=letterCount(entry.word||entry.lemma),age=ageBandCandidateForEntry(entry);if(!age)return 'lexique_background';if(frequency>=120&&syllables<=2&&letters<=9)return 'very_common_simple';if(frequency>=40&&syllables<=3&&letters<=12)return 'child_common';if(frequency>=15&&syllables<=4&&letters<=15)return 'school_common';if(frequency>=5&&syllables<=5&&letters<=18)return 'teen_adult_common';if(frequency>=1.5&&syllables<=5&&letters<=18)return 'advanced_useful';return 'lexique_background';}
export function rebuloUtilityWeight(target={}){return Number(target?.rebuloUtilityWeight??UTILITY_WEIGHTS[target.rebuloUtilityTier]??0);}
export function isRebuloPriorityTarget(target={}){return rebuloUtilityWeight(target)>0;}

function candidateEntry(raw={}){const word=String(raw.word||'').trim(),lemma=String(raw.lemma||word).trim(),ipa=normalizeIPA(raw.ipa||''),syllableCount=Number.isInteger(raw.syllableCount)&&raw.syllableCount>0?raw.syllableCount:null,syllables=sourceSyllables(raw.syllabification||''),ageBandCandidate=ageBandCandidateForEntry(raw);if(!ageBandCandidate)return null;const rebuloUtilityTier=rebuloUtilityTierForEntry(raw);return {target:word,lemma,targetIpa:ipa,pos:raw.pos||'',frequency:Number(raw.frequency)||0,syllableCount,syllables,syllabificationSource:raw.syllabification||null,syllabificationStatus:syllables.length===syllableCount?'source_exact':'needs_source_review',ageBandCandidate,ageStatus:'heuristic_preselection',rebuloUtilityTier,rebuloUtilityWeight:UTILITY_WEIGHTS[rebuloUtilityTier]??0,utilityStatus:'heuristic_product_preselection',schoolFrequencyEvidence:null,source:'Lexique 4'};}
export function buildTargetVocabulary(entries=[]){const groups=new Map();for(const raw of entries||[]){const item=candidateEntry(raw);if(!item)continue;const lemmaKey=normalizedWord(item.lemma);if(!lemmaKey)continue;let group=groups.get(lemmaKey);if(!group){group={best:null,pronunciations:new Map()};groups.set(lemmaKey,group);}const isLemmaForm=normalizedWord(item.target)===lemmaKey,previous=group.best;if(!previous||(isLemmaForm&&!previous.isLemmaForm)||(isLemmaForm===previous.isLemmaForm&&item.frequency>previous.item.frequency))group.best={item,isLemmaForm};let pronunciation=group.pronunciations.get(item.targetIpa);if(!pronunciation){pronunciation={ipa:item.targetIpa,forms:[],maxFrequency:0};group.pronunciations.set(item.targetIpa,pronunciation);}if(!pronunciation.forms.includes(item.target))pronunciation.forms.push(item.target);pronunciation.maxFrequency=Math.max(pronunciation.maxFrequency,item.frequency);}return [...groups.values()].filter(g=>g.best).map(g=>{const primary=g.best.item;const pronunciationVariants=[...g.pronunciations.values()].filter(v=>v.ipa!==primary.targetIpa).sort((a,b)=>b.maxFrequency-a.maxFrequency||a.ipa.localeCompare(b.ipa));return {...primary,pronunciationVariants};}).sort((a,b)=>b.rebuloUtilityWeight-a.rebuloUtilityWeight||a.ageBandCandidate-b.ageBandCandidate||b.frequency-a.frequency||a.target.localeCompare(b.target,'fr'));}

export function applySchoolFrequencyEvidence(targets=[],dataset={}){
  const evidence=new Map((dataset?.entries||[]).map(item=>[normalizedWord(item.word),item]));
  return (targets||[]).map(target=>{
    const hit=evidence.get(normalizedWord(target.lemma))||evidence.get(normalizedWord(target.target));
    if(!hit)return target;
    const currentWeight=rebuloUtilityWeight(target),promotedWeight=Math.max(currentWeight,UTILITY_WEIGHTS.school_common);
    const promotedTier=currentWeight>=UTILITY_WEIGHTS.school_common?target.rebuloUtilityTier:'school_common';
    return {...target,rebuloUtilityTier:promotedTier,rebuloUtilityWeight:promotedWeight,utilityStatus:'heuristic_plus_public_school_frequency',schoolFrequencyEvidence:{source:'eduscol_brunet_frequency_1500',rank:Number(hit.rank)||null,frequency:Number(hit.frequency)||null,license:dataset?.license||'Licence Ouverte / Open Licence Etalab 2.0'}};
  }).sort((a,b)=>b.rebuloUtilityWeight-a.rebuloUtilityWeight||a.ageBandCandidate-b.ageBandCandidate||b.frequency-a.frequency||a.target.localeCompare(b.target,'fr'));
}

export function buildSyllableInventory(targets=[]){const inventory=new Map();for(const target of targets||[]){if(target?.syllabificationStatus!=='source_exact')continue;for(const syllable of target.syllables||[]){const ipa=normalizeIPA(syllable);if(!ipa)continue;let item=inventory.get(ipa);if(!item){item={ipa,targetCount:0,totalFrequency:0,minAgeBandCandidate:12,examples:[]};inventory.set(ipa,item);}item.targetCount++;item.totalFrequency+=Number(target.frequency)||0;item.minAgeBandCandidate=Math.min(item.minAgeBandCandidate,Number(target.ageBandCandidate)||12);if(item.examples.length<8&&!item.examples.includes(target.target))item.examples.push(target.target);}}return [...inventory.values()].map(i=>({...i,totalFrequency:Number(i.totalFrequency.toFixed(3))})).sort((a,b)=>b.targetCount-a.targetCount||b.totalFrequency-a.totalFrequency||a.ipa.localeCompare(b.ipa));}
export function targetVocabularyStats(targets=[]){const ageBands=Object.fromEntries(TARGET_AGE_BANDS.map(p=>[String(p.age),0])),utilityTiers=Object.fromEntries(REBULO_UTILITY_TIERS.map(t=>[t,0]));let sourceExactSyllabification=0,needsSourceReview=0,alternatePronunciations=0,schoolFrequencyEvidenceCount=0;for(const target of targets||[]){const key=String(target?.ageBandCandidate||'');if(key in ageBands)ageBands[key]++;if(target?.rebuloUtilityTier in utilityTiers)utilityTiers[target.rebuloUtilityTier]++;if(target?.syllabificationStatus==='source_exact')sourceExactSyllabification++;else needsSourceReview++;if(target?.schoolFrequencyEvidence)schoolFrequencyEvidenceCount++;alternatePronunciations+=Array.isArray(target?.pronunciationVariants)?target.pronunciationVariants.length:0;}return {total:targets.length,ageBands,utilityTiers,rebuloPriorityTotal:targets.filter(isRebuloPriorityTarget).length,lexiqueBackgroundTotal:targets.filter(t=>t.rebuloUtilityTier==='lexique_background').length,schoolFrequencyEvidenceCount,sourceExactSyllabification,needsSourceReview,alternatePronunciations};}
